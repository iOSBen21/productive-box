name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ secrets.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 }}
          GIST_ID: ${{ secrets.5c245c8ffda690a22d2fa51c49f1f2af }}
          TIMEZONE: Asia/Seoul
