name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ secrets.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 }}
          GIST_ID: ${{ secrets.4e32a37a7f9c2f96cb49a71aba0d791a }}
          TIMEZONE: Asia/Seoul
